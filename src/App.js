import React from 'react';
import {PostContainer} from "./components/PostContainer/PostContainer";

const App = () => {
    // # 1 додати в package.json бібліотеку react-hook-form
    return (
        <div>
            <PostContainer/>
        </div>
    );
};

export default App;